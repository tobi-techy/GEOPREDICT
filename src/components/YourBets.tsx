'use client';

import type { Market } from '@/lib/markets';

// Stubbed — Aleo wallet removed, will be replaced with Fhenix on-chain reads
export default function YourBets({ markets: _ }: { markets: Market[] }) {
  return null;
}
